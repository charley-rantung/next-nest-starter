/**
 * Private Constant
 *
 * Environment variable yang hanya bisa diakses di sisi server
 */

export const NODE_ENV = process.env.NODE_ENV
export const JWT_ACCESS_TOKEN_SECRET = process.env.JWT_ACCESS_TOKEN_SECRET
